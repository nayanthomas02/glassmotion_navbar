name: glassmotion_navbar_example
description: Example app for glassmotion_navbar package.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  glassmotion_navbar:
    path: ../   # <– points to your local package

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
